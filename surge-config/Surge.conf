# RETIRED PUBLIC SURGE PROFILE
# Sensitive Surge configuration has been moved to a private repository.
# This public file intentionally contains no nodes, subscriptions, credentials, or MITM material.

[General]
loglevel = notify

[Rule]
FINAL,DIRECT
