# Quick Reply Flow

Target interaction:

Chat screen -> background pre-generation -> tap `快捷回复` -> show 3 cached AI replies immediately -> tap one reply -> fill WeChat input -> manual send.

The original MiYou dependency chain remains intact. This change only moves AI generation earlier and hides the add/plus affordance on the quick-reply list; it does not delete shared dependencies.
