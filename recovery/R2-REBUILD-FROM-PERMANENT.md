# R2 一键重建规则

触发口令：**重建R2**

1. 永不动主配置固定使用提交 `06c27a9f8823ba61b99b522b5d6c15168234bad3`，不得修改。
2. 正式模块来自 `stable/surge-permanent-modules` 当前头；允许新增已验证模块。
3. 从上述“固定主配置 + 当前正式模块库”生成新的 `work/surge-r2-current`。
4. R2 是唯一日常/研究版，可修改主配置和模块。
5. `main` 与 `surge-stable` 始终跟随当前 R2。
6. 重新部署日常 Vercel 两个固定入口并标记 `R2-current`。
7. 永久冻结 URL、永不动主配置提交和历史保险分支均不得覆盖。
8. 旧 8/30、R1、旧 R2、纠错备份只做历史材料，默认恢复时禁止使用。
